<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <%= link_to root_path do %>
          <%= image_tag('logo.jpg', size: '100') %>
        <% end %>
      </ul>
      <ul class="navbar-nav navbar-right">
        <%= link_to 'Пробный тест', sample_test_path, class: 'btn btn-default navbar-btn' %>
        <%= link_to 'Наши преподаватели', teachers_path, class: 'btn btn-default navbar-btn' %>
        <%= link_to 'Цена', content_path(2), class: 'btn btn-default navbar-btn' %>
        <%= link_to 'Методика', content_path(3), class: 'btn btn-default navbar-btn' %>
        <%= link_to 'Контакты', content_path(4), class: 'btn btn-default navbar-btn' %>
        <%= link_to 'О нас', content_path(5), class: 'btn btn-default navbar-btn' %>
        <%= link_to 'FAQ', content_path(6), class: 'btn btn-default navbar-btn' %>
        <% if user_signed_in? %>
          <p class="navbar-text">
            Привет, <%= current_user.name %>!
          </p>
          <ul class="nav navbar-nav">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Аккаунт <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to 'Редактировать профиль', edit_user_registration_path(current_user) %></li>
                <li class="divider"></li>
                <li><%= link_to 'Выйти', destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
          </ul>
          <%= link_to 'Список литературы', literature_list_path, class: 'btn btn-default navbar-btn' %>
           <% if current_user.position_id == 1 %>
            <%= link_to 'Личный кабинет', student_cabinet_path(current_user), class: 'btn btn-default navbar-btn' %>
           <% elsif current_user.position_id == 2 %>
            <%= link_to 'Личный кабинет', teacher_cabinet_path, class: 'btn btn-default navbar-btn' %>
           <% end %>
        <% else %>
          <%= link_to 'Список литературы', literature_list_path, class: 'btn btn-default navbar-btn' %>
          <%= link_to 'Регистрация', new_user_registration_path, class: 'btn btn-default navbar-btn' %>
          <%= link_to 'Войти', new_user_session_path, class: 'btn btn-default navbar-btn' %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
