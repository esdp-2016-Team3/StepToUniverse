<h1>Список литературы</h1>
<div class="row">
  <% @library_files.each do |library_file| %>
      <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
          <div class="caption">
            <h3>Название литературы: <span class="for_green_words"><%= library_file.name %></h3>
            <h3>Автор: <span class="for_red_words""><%= User.find(library_file.user_id).name %></span></h3>
            <h3>Описание: <span class="for_blue_words"><%= library_file.description %></span></h3>
            <% if user_signed_in? %>
              <% unless library_file.literature_file_name == nil  %>
                <h3>Файл: <span class="for_special_color_word_2"><%= library_file.literature_file_name%></span>/
                                <%= link_to "Скачать", '/library_files/' + library_file.literature_file_name.to_s,
                                                                 download: library_file.literature_file_name.to_s %></h3>
              <% end %>
            <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>